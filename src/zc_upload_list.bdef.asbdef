projection;
strict ( 2 );
use draft;

define behavior for ZC_UPLOAD_LIST alias List
{
  use create;
  use update;
  use delete;

  use action downloadTemplate;
  //  use action uploadData;
  use action ExcelUpload;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}